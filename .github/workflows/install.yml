name: "ci: Build Install Media image"
on:
  workflow_dispatch:
  schedule:
    - cron: "0 0 1 * *"
  push:
    tags:
      - "*"
env:
  NIX_SHOW_STATS: 1
jobs:
  image:
    runs-on: ubuntu-latest
    steps:
      - name: Clone Repo
        uses: actions/checkout@v5
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          fetch-depth: 0
      - name: Install Nix
        uses: cachix/install-nix-action@v31
        with:
          extra_nix_config: |
            experimental-features = nix-command flakes
            accept-flake-config = true
            access-tokens = github.com=${{ secrets.GITHUB_TOKEN }}
      - name: Build Default Install Media
        run: nix build .#nixosConfigurations.install.config.system.build.images.iso -L --show-trace
      - name: Upload ISO Image
        uses: actions/upload-artifact@v4
        id: image-upload
        with:
          name: Image
          path: result/iso/install.iso
          compression-level: 0
          if-no-files-found: error
