### Dotfiles

The `files` directory is used to store all my plain-text program configuration files
_(or as we all love to call it, the `dotfiles`)_

Read [`_module.nix`](./_module.nix) to understand how they are used
